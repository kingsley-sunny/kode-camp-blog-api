class TaskController {
  create(req, res) {
    res.json({ message: "Login successful" });
  }

  find(req, res) {
    res.json({ message: "Register Successful successful" });
  }
}

module.exports = TaskController;
